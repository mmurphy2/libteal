# Error testing: this file contains some invalid components that iniparse
# should be rejecting.

# No .. segments in section names
[foo/../bar]
message = This should not be written

[../foo]
message = Same thing: should not be written

[foo/..]
message = Also bad

[valid]
a valid key = ok
an invalid / key = no slashes allowed
/ also invalid = bad
also also invalid / = also bad

; Let's throw up a few syntax errors for fun
---
invalid
[
]
